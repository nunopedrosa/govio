This directory is populated on the server by:

  php tools/fetch_mediabunny.php

It downloads the pinned Mediabunny browser-global builds from the official
GitHub release configured in config.php. The application does not use a CDN
at runtime.
